[project]
name = "lenet-mnist-quickdraw"
version = "0.1.0"
description = "LeNet-5 trained on MNIST or Quick, Draw! with a Tkinter GUI"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.23",
    "pillow>=9.0",
    "flask>=3.0",
    "torch>=2.0",
    "torchvision>=0.15",
]

[tool.uv]
package = false
